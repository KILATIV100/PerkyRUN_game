# 🚀 Покрокова інструкція деплою на Railway

## 📋 Чек-ліст перед деплоєм

- [ ] Код залито на GitHub
- [ ] Акаунт Railway створено
- [ ] Telegram бот створено через @BotFather
- [ ] Отримано BOT_TOKEN

---

## 1️⃣ Створення Telegram бота

### Крок 1: Створіть бота

1. Відкрийте [@BotFather](https://t.me/BotFather) в Telegram
2. Відправте команду: `/newbot`
3. Введіть назву бота (наприклад: `PerkUP Runner`)
4. Введіть username бота (має закінчуватись на `bot`, наприклад: `perkup_runner_bot`)
5. **Збережіть токен** - він виглядає так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### Крок 2: Налаштуйте бота

Відправте наступні команди в @BotFather:

```
/setdescription
```
Введіть опис:
```
PerkUP Runner - біжи вулицями Броварів та збирай кавові зерна! ☕🏃
```

```
/setabouttext
```
Введіть:
```
Офіційна гра PerkUP кафе в Броварях. Збирай кавові зерна та отримуй знижки!
```

```
/setcommands
```
Введіть список команд:
```
start - Почати гру
stats - Моя статистика
leaderboard - Таблиця лідерів
help - Допомога
```

### Крок 3: Додайте іконку (опціонально)

```
/setuserpic
```
Завантажте квадратну картинку (рекомендовано 512x512px)

---

## 2️⃣ Деплой на Railway

### Крок 1: Підготовка GitHub репозиторію

```bash
# Якщо ще не ініціалізували git
git init
git add .
git commit -m "Initial commit: PerkUP Runner"

# Створіть репозиторій на GitHub і завантажте код
git remote add origin https://github.com/ваш-username/PerkyRUN_game.git
git branch -M main
git push -u origin main
```

### Крок 2: Створення проєкту на Railway

1. Перейдіть на [railway.app](https://railway.app)
2. Натисніть **"Start a New Project"**
3. Виберіть **"Deploy from GitHub repo"**
4. Авторизуйте GitHub (якщо ще не зробили)
5. Виберіть ваш репозиторій `PerkyRUN_game`
6. Railway автоматично почне деплой

### Крок 3: Додавання PostgreSQL бази даних

1. В Railway проєкті натисніть **"+ New"**
2. Виберіть **"Database"**
3. Виберіть **"Add PostgreSQL"**
4. Railway автоматично створить базу і додасть змінну `DATABASE_URL`

### Крок 4: Налаштування змінних оточення

1. Клікніть на ваш сервіс
2. Перейдіть на вкладку **"Variables"**
3. Додайте наступні змінні:

```env
NODE_ENV=production
PORT=3000
TELEGRAM_BOT_TOKEN=ваш_токен_від_botfather
TELEGRAM_WEBAPP_URL=https://ваш-проект.up.railway.app
JWT_SECRET=випадковий_секретний_ключ_мінімум_32_символи
ALLOWED_ORIGINS=https://web.telegram.org
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
```

**Як отримати TELEGRAM_WEBAPP_URL:**
- Після першого деплою Railway надасть вам URL
- Перейдіть в Settings → Domains
- Скопіюйте URL (наприклад: `https://perkuprunner.up.railway.app`)
- Додайте цей URL як значення `TELEGRAM_WEBAPP_URL`

**Як згенерувати JWT_SECRET:**
```bash
# В терміналі:
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### Крок 5: Міграція бази даних

Після успішного деплою виконайте міграцію БД:

**Варіант 1: Через Railway CLI**
```bash
# Встановіть Railway CLI
npm i -g @railway/cli

# Авторизуйтесь
railway login

# Підключіться до проєкту
railway link

# Запустіть міграцію
railway run npm run db:migrate
```

**Варіант 2: Через веб-інтерфейс**
1. Відкрийте ваш сервіс в Railway
2. Перейдіть на вкладку **"Deployments"**
3. Знайдіть останній деплой і клікніть три крапки **"..."**
4. Виберіть **"View Logs"** або **"Run Command"**
5. Введіть команду: `npm run db:migrate`
6. Дочекайтесь успішного виконання

### Крок 6: Створення окремого сервісу для бота

Railway вимагає окремий сервіс для бота:

1. В проєкті натисніть **"+ New"** → **"Empty Service"**
2. Назвіть його `Bot`
3. Перейдіть в **Settings**
4. В **Source** підключіть той самий GitHub репозиторій
5. В **Root Directory** залиште пустим
6. В **Start Command** введіть: `npm run bot`
7. В **Variables** додайте ТІ САМІ змінні, що й для головного сервісу
8. Збережіть та задеплойте

### Крок 7: Перевірка деплою

Перевірте чи працює:

1. **API Health Check:**
   ```
   https://ваш-проект.up.railway.app/api/health
   ```
   Має повернути JSON з статусом "OK"

2. **Бот:**
   - Відкрийте вашого бота в Telegram
   - Відправте `/start`
   - Якщо бот відповідає - все працює!

---

## 3️⃣ Підключення Web App до бота

### Крок 1: Створіть Web App

1. В [@BotFather](https://t.me/BotFather) виберіть вашого бота
2. Відправте команду: `/newapp`
3. Виберіть вашого бота зі списку
4. Введіть назву: `PerkUP Runner`
5. Введіть опис: `Бігай вулицями Броварів та збирай кавові зерна!`
6. Завантажте **фото** (640x360px) - скріншот гри або лого
7. Завантажте **GIF/відео** (опціонально) - демо гри
8. Введіть **Web App URL**: `https://ваш-проект.up.railway.app`
9. Завантажте **іконку** для кнопки (опціонально)

### Крок 2: Тестування

1. Відкрийте бота в Telegram
2. Натисніть кнопку **"🎮 Грати"**
3. Має відкритися гра
4. Зіграйте тестову гру
5. Перевірте чи зберігаються результати

---

## 4️⃣ Перевірка функціоналу

### Чек-ліст тестування:

- [ ] Бот відповідає на `/start`
- [ ] Кнопка "Грати" відкриває гру
- [ ] Гра завантажується без помилок
- [ ] Свайпи працюють (вліво/вправо/вгору/вниз)
- [ ] Збір монет і зерен працює
- [ ] Game Over відображається
- [ ] Результат зберігається в БД
- [ ] `/stats` показує правильну статистику
- [ ] `/leaderboard` показує таблицю лідерів
- [ ] Магазин працює
- [ ] Покупка предметів працює

---

## 5️⃣ Моніторинг та логи

### Перегляд логів

**Railway Dashboard:**
1. Відкрийте ваш сервіс
2. Перейдіть на вкладку **"Deployments"**
3. Клікніть на активний деплой
4. Дивіться логи в реальному часі

**Railway CLI:**
```bash
railway logs
```

### Корисні команди

```bash
# Перезапустити сервіс
railway restart

# Перезапустити конкретний сервіс
railway restart --service=web

# Переглянути змінні
railway variables

# Додати змінну
railway variables set KEY=value

# Підключитися до БД
railway connect postgresql
```

---

## 6️⃣ Налаштування домену (опціонально)

### Custom Domain

1. В Railway перейдіть в **Settings → Domains**
2. Натисніть **"Custom Domain"**
3. Введіть ваш домен (наприклад: `game.perkup.com.ua`)
4. Railway покаже DNS записи які потрібно додати
5. Додайте CNAME запис в вашого DNS провайдера:
   ```
   CNAME game.perkup.com.ua -> ваш-проект.up.railway.app
   ```
6. Дочекайтесь поширення DNS (може зайняти до 24 годин)
7. Оновіть `TELEGRAM_WEBAPP_URL` на новий домен

---

## 7️⃣ Troubleshooting (Вирішення проблем)

### Проблема: Бот не відповідає

**Рішення:**
1. Перевірте чи запущений сервіс `Bot` в Railway
2. Перегляньте логи: `railway logs --service=bot`
3. Перевірте `TELEGRAM_BOT_TOKEN` в змінних
4. Перезапустіть бота: `railway restart --service=bot`

### Проблема: Database connection error

**Рішення:**
1. Перевірте чи створена PostgreSQL база в Railway
2. Перевірте чи є змінна `DATABASE_URL`
3. Запустіть міграцію: `railway run npm run db:migrate`
4. Перезапустіть сервіс

### Проблема: WebApp не відкривається

**Рішення:**
1. Перевірте `TELEGRAM_WEBAPP_URL` - має бути `https://`
2. Перевірте чи доступний URL в браузері
3. Очистіть кеш Telegram: Settings → Data and Storage → Clear Cache

### Проблема: CORS errors

**Рішення:**
1. Переконайтесь що `ALLOWED_ORIGINS` містить `https://web.telegram.org`
2. Перезапустіть сервіс після зміни змінних

### Проблема: 500 Internal Server Error

**Рішення:**
1. Перегляньте логи сервера
2. Перевірте чи всі змінні оточення налаштовані
3. Перевірте чи база даних підключена
4. Запустіть міграції якщо ще не зробили

---

## 8️⃣ Оновлення коду

При внесенні змін в код:

```bash
# 1. Закомітьте зміни
git add .
git commit -m "Опис змін"

# 2. Запуште на GitHub
git push origin main

# 3. Railway автоматично задеплоїть нову версію!
```

Railway автоматично:
- Виявляє нові коміти
- Білдить проєкт
- Деплоїть нову версію
- Перезапускає сервіси

---

## 9️⃣ Backup бази даних

### Створення backup

```bash
# Підключіться до Railway
railway link

# Створіть дамп БД
railway run pg_dump $DATABASE_URL > backup_$(date +%Y%m%d).sql
```

### Відновлення з backup

```bash
railway run psql $DATABASE_URL < backup_20240101.sql
```

---

## 🎉 Готово!

Ваш PerkUP Runner тепер запущений на Railway!

### Корисні посилання:

- 📚 [Railway Docs](https://docs.railway.app/)
- 🤖 [Telegram Bot API](https://core.telegram.org/bots/api)
- 🎮 [Telegram WebApps](https://core.telegram.org/bots/webapps)

### Наступні кроки:

1. Поділіться ботом з друзями для тестування
2. Додайте аналітику (Google Analytics, Amplitude)
3. Налаштуйте сповіщення про помилки (Sentry)
4. Додайте нові фічі з TODO списку
5. Збирайте фідбек від користувачів

**Успіхів! ☕🏃**
